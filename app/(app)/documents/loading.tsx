import { RouteLoading } from '@/components/ui/RouteLoading'

export default function Loading() {
  return <RouteLoading title="Opening Documents and POs" subtitle="Reading the packets you have saved and what each one proves…" shape="cards" />
}
