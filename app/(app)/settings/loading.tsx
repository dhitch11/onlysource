import { RouteLoading } from '@/components/ui/RouteLoading'

export default function Loading() {
  return <RouteLoading title="Opening alerts and notifications" subtitle="Reading your channels, per-signal switches and recipient…" shape="cards" />
}
