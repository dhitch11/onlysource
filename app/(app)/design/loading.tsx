import { RouteLoading } from '@/components/ui/RouteLoading'

export default function Loading() {
  return (
    <RouteLoading
      title="Opening the design system"
      subtitle="Rendering every component in every state so a change can be judged before it ships…"
      shape="cards"
    />
  )
}
